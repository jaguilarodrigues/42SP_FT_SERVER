# 42SP_FT_SERVER

### Configuração NGINX

Atualizando

```console
apt-get update && apt-get install -y procps
```
Instala o NGINX

```console
apt-get install nginx
```

Ver processos com nginx

```console
ps -ef | grep nginx
```

